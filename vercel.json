{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "git lfs pull && npm run build"
        }
      }
    ]
  }
  